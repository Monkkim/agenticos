# Learnings Log

Skill별 피드백이 누적되는 **학습 로그**. `ops/wrap-up`이 세션 끝마다 이 파일에 append하고, 중요한 학습은 해당 skill의 `SKILL.md`에 반영한다.

## 사용 규칙

1. 모든 skill은 실행 전 **자기 섹션을 Read**한다 — 과거 피드백을 무시하지 않기 위함.
2. `wrap-up`이 피드백을 기록할 때 **날짜 + 한 줄 요약 + (선택) 재현 예시** 구조를 지킨다.
3. 같은 학습이 3회 이상 반복되면 → 해당 skill의 `SKILL.md`에 공식 규칙으로 승격.
4. Skill이 삭제되면 해당 섹션도 같이 제거 (`ops/heartbeat`이 감지).

---

## foundation/start-here

- 2026-04-21: 최초 온보딩 완료. 도메인=개인 브랜드/지식 창업 선택, brand-context/ 5개 파일 v0.1 생성.
  - 맥락: 사용자가 여러 질문에 "아직 정리 안 됨"으로 답변(Pain 구체화·반직관 조언·시그니처 에피소드) → 억지 채움 대신 `(미입력)` 마커로 남기고 진행.
  - 교훈: 인터뷰 완결성보다 "즉시 사용 가능한 초안" 생성이 우선. 미입력 구간은 명시하고 후속 세션에 갱신.

## foundation/brand-voice-extraction

- 2026-04-21: 실제 글 샘플 없이 인터뷰 기반 v0.1 생성. 사용자가 샘플(호라이즌프레스 인스타) 공유 시점에 "SNS 본문 마침표 생략" 규칙 추가로 확보 → voice.md에 즉시 반영.
  - 교훈: 샘플 하나라도 확보되면 voice 정밀도가 크게 올라간다. 샘플 요청을 Stage 3가 아니라 Stage 1~2 사이로 앞당기면 효율 증가 가능.

## foundation/icp-builder

- 2026-04-21: Pain 구체화는 사용자 답변에 없었지만 "AI에 적응하고 싶은 30~40대"라는 정체성 답변 하나로 Pain/Desired를 합리적으로 확장 작성 가능. voice-of-customer 섹션은 실데이터 확보 전까지 템플릿만 남김.

## foundation/positioning-builder

- 2026-04-21: 사용자가 한 줄 포지셔닝 단일 확정 대신 "세 개 모두 보존" 선택. 공식형/친근형/도발형 3후보를 채널별로 변주 사용하는 전략 — 추후 사용 후 승자 정해도 OK.

## ops/heartbeat

_(세션 피드백 없음)_

## ops/wrap-up

- 2026-04-21: 첫 wrap-up 실행. heartbeat가 자동 발화되지 않아 오늘 memory 파일이 없던 상태 → wrap-up이 신규 생성.
  - 맥락: SessionStart 훅이 기대한 자동 heartbeat이 실제로 돌지 않은 사례.
  - 교훈: 다음 세션 시작 시 수동으로 heartbeat 확인 필요. 훅 설정/메시지 검증을 다음 세션의 첫 TODO에 추가 고려.

## ops/skill-creator

- 2026-04-30: `execution/script-to-newsletter` 신규 생성. 사용자가 발송 뉴스레터 6편을 직접 공유 → 패턴을 references/newsletter-pattern.md로 추출 후 SKILL.md에 12블록 골격 박음.
  - 교훈: 실제 발송분 샘플이 있으면 패턴 추출이 빠르고 정확. "예시 보여달라"가 skill-creator 1단계의 효율적 경로.

## execution/script-to-newsletter

_(세션 피드백 없음 — 2026-04-30 신규 생성. 첫 발송 후 사용자 수정 요청을 여기에 기록)_

## execution/repurpose

- 2026-05-09: 신규 생성. Nicholas Puru `/repurpose` 가이드를 한국화 + 데이븐포트 voice에 맞춰 적응. LinkedIn/X 제외하고 인스타·쓰레드·숏폼훅 3종 + 옵션 뉴스레터(`script-to-newsletter` 12블록 위임) 구조.
  - 맥락: 사용자 요구로 플랫폼 3개로 축소, 뉴스레터는 출력 카테고리 분리.
  - 교훈: 글로벌 `~/.claude/commands/`가 아니라 프로젝트 로컬 `.claude/commands/`에 슬래시 커맨드를 두는 것이 AgenticOS 원칙(글로벌 독립)과 일치.

- 2026-05-09: **인스타 산출물 = 릴스 영상 대본** (피드 캡션 아님). 사용자 본인 릴스 대본 7편 공유받아 톤 추출.
  - 맥락: 첫 `/repurpose` 실행 후 사용자가 본인 실제 대본 7개 공유 ("이런식으로 이루어 지고 있어 참고해줘"). 시스템 기본값이었던 피드 캡션 800~1500자 호라이즌프레스 스타일과 사용자 실 운영(릴스 대본, 한 줄 한 호흡) 사이 큰 갭 발견.
  - 교훈:
    1. 인스타 산출물은 **릴스 영상 대본 형식**이 기본. 한 줄 = 한 호흡, 본문 중간 마침표도 자주 생략.
    2. 후킹 공식 5종은 `samples.md` 7편에서 추출 — 신규 인스타 작성 시 가장 가까운 공식을 1차 후보로.
    3. 본인 표현(`요물` / `이게 진짜 ~인 게` / `심지어` / `이야 진짜` / `장난 아니던데` / `굉장히 편하겠죠?`) 적극 차용. AI 표준 어휘 회피.
    4. CTA는 댓글 이벤트형/DM 발송형/시청 유지형/채널 유도형 4종 분기. 영상 성격 따라 택1.
    5. **반영 파일**: `samples.md`(6편 카탈로그), `voice.md`(자주 쓰는 표현 + 릴스 대본 모드 섹션), `platform-formats.md`(인스타 섹션 릴스 대본 기준 재작성).

- 2026-05-09 (3차 보정): **분량 룰 200~280자 / 발화 30~45초 절대화**. 1차 산출물이 600자 넘어 호흡 두 배 분량으로 빠짐.
  - 맥락: 사용자가 본인 6편 대본을 `----`로 구분해 다시 보여주며 "대본이 저렇게 짧아야해" 명시. 6편 실측 분량 200~280자.
  - 교훈:
    1. **한 영상 = 한 인사이트**. 원본 유튜브가 다중 메시지(격리/메가/시스템 3개)를 다뤄도, 릴스로 옮길 땐 가장 강한 1개만 골라 압축.
    2. 250자 넘기면 릴스 호흡이 깨진다. 분량 검증을 0단계 의존성 Read 직후 체크리스트에 박을 것.
    3. **클로징은 cliffhanger형(50%) / CTA 직접형(17%) / 공감형(33%) 비율**. 시리즈성 영상엔 cliffhanger 적극 차용.
    4. 사용자 분량 지적이 1편 = 6편이라는 사실(`----` 구분자) 정정 동반 — 카탈로그 7편 → 6편으로 수정.

## strategy/youtube-audit

_(세션 피드백 없음 — 2026-05-09 신규 생성. 비어있던 strategy/ 카테고리 첫 입주. 첫 audit 실행 후 데이터 신뢰도·임계값(0.5/2배) 조정 요청을 여기에 기록)_

## strategy/script-scoring

_(세션 피드백 없음 — 2026-05-09 신규 생성. 첫 채점 실행 후 4축 가중치·리라이트 톤 수정 요청을 여기에 기록)_
